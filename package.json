{
  "name": "simcity-threejs-clone",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 3333",
    "server": "node server/index.js",
    "dev:full": "concurrently \"npm run dev\" \"npm run server\"",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^4.4.5",
    "concurrently": "^8.2.0"
  },
  "dependencies": {
    "three": "^0.155.0",
    "express": "^4.18.0",
    "ws": "^8.14.0"
  }
}
